test('arbitrary', () => {
  /*
  Adding something arbitrary here so that CI can pass 
  instead of actually modifying the CI because projen is not 
  very compatible with editing individual task steps.
  
  Could patch it but maybe what I should do instead is make a PR
  to projen that adds the functionality.
  */
});
